import 'package:flutter/material.dart';

class TargetLabel extends StatelessWidget {
  const TargetLabel({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('子が狙ったマッチ度　??');
  }
}
