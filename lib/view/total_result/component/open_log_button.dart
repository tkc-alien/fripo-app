import 'package:flutter/material.dart';

class OpenLogButton extends StatelessWidget {
  const OpenLogButton({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return OutlinedButton(
      onPressed: () {},
      child: const Text('履歴を見る'),
    );
  }
}
