import 'package:flutter/cupertino.dart';

class AnsweringFragment extends StatelessWidget {
  const AnsweringFragment({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Answering'),
    );
  }
}
